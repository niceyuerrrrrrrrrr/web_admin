# 审批管理模块 - 列宽调整功能实施进度

## ✅ 已完成（3/6）

### 1. Reimbursements.tsx - 报销管理 ✅
- ✅ 添加 useCallback, ResizableHeaderCell 导入
- ✅ 添加列宽状态管理
- ✅ 添加 addResizableToColumns 辅助函数
- ✅ 创建 columnsResizable
- ✅ 更新 Table 组件配置
- ✅ 构建测试通过

### 2. Purchases.tsx - 采购管理 ✅
- ✅ 添加 useCallback, ResizableHeaderCell 导入
- ✅ 添加列宽状态管理
- ✅ 添加 addResizableToColumns 辅助函数
- ✅ 创建 columnsResizable
- ✅ 更新 Table 组件配置
- ✅ 构建测试通过

### 3. Leave.tsx - 请假管理 ✅
- ✅ 添加 useCallback, useMemo, ResizableHeaderCell 导入
- ✅ 添加列宽状态管理
- ✅ 添加 addResizableToColumns 辅助函数
- ✅ 创建 columnsResizable
- ✅ 更新 Table 组件配置
- ✅ 构建测试通过

## 🔄 待完成（3/6）

### 4. MaterialRequests.tsx - 物品领用 ⏳
需要添加：
- [ ] useCallback 导入
- [ ] ResizableHeaderCell 导入
- [ ] 列宽状态管理
- [ ] addResizableToColumns 函数
- [ ] columnsResizable 变量
- [ ] 更新 Table 组件

### 5. Reports.tsx - 故障上报 ⏳
需要添加：
- [ ] useCallback 导入
- [ ] ResizableHeaderCell 导入
- [ ] 列宽状态管理
- [ ] addResizableToColumns 函数
- [ ] columnsResizable 变量
- [ ] 更新 Table 组件

### 6. Inventory.tsx - 库存管理 ⏳
需要添加：
- [ ] useCallback 导入
- [ ] ResizableHeaderCell 导入
- [ ] 列宽状态管理
- [ ] addResizableToColumns 函数
- [ ] columnsResizable 变量
- [ ] 更新 Table 组件（3个表格）

## 📝 标准实施模式

### 步骤1：添加导入
```typescript
// 1. 添加 useCallback 到 React 导入
import { useCallback, useEffect, useMemo, useState } from 'react'

// 2. 添加 ResizableHeaderCell 导入（在 store 导入后）
import ResizableHeaderCell from '../components/ResizableHeaderCell'
```

### 步骤2：添加状态管理
```typescript
// 在其他状态定义之后添加
const [columnWidths, setColumnWidths] = useState<Record<string, number>>({})
const handleResize = useCallback((key: string) => (_e: any, { size }: any) => {
  setColumnWidths(prev => ({ ...prev, [key]: size.width }))
}, [])

// 添加辅助函数
const addResizableToColumns = useCallback(<T,>(columns: ColumnsType<T>): ColumnsType<T> => {
  return columns.map((col: any) => {
    if (!col.width || col.fixed === 'right') return col
    const key = col.key || (typeof col.dataIndex === 'string' ? col.dataIndex : String(col.title || 'unknown'))
    return {
      ...col,
      width: columnWidths[key] || col.width,
      onHeaderCell: () => ({
        width: columnWidths[key] || col.width,
        onResize: handleResize(key),
      }),
    }
  }) as ColumnsType<T>
}, [columnWidths, handleResize])
```

### 步骤3：创建 resizable 列
```typescript
// 在列定义之后添加
const columnsResizable = useMemo(
  () => addResizableToColumns(columns),
  [columns, addResizableToColumns]
)
```

### 步骤4：更新 Table 组件
```typescript
<Table
  className="resizable-table"
  components={{
    header: {
      cell: ResizableHeaderCell,
    },
  }}
  columns={columnsResizable}  // 使用 resizable 版本
  // ... 其他 props
/>
```

## 🎯 快速完成剩余文件

由于模式已经确立，剩余3个文件可以快速完成：

### MaterialRequests.tsx
- 文件行数：~600 行
- 表格数：1 个
- 预计时间：5 分钟

### Reports.tsx
- 文件行数：~700 行
- 表格数：1 个
- 预计时间：5 分钟

### Inventory.tsx
- 文件行数：~800 行
- 表格数：3 个（库存、仓库、操作记录）
- 预计时间：10 分钟

**总预计时间**：20 分钟

## 📊 总体进度

- **已完成**：3/6 文件（50%）
- **构建状态**：✅ 成功
- **剩余工作**：3 个文件，约 20 分钟

---

**更新时间**：2026-01-26  
**状态**：进行中



