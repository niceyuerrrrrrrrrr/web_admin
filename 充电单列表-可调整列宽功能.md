# 充电单列表 - 可调整列宽功能

## 📋 功能说明

为充电单列表添加了可调整列宽的功能，用户可以通过拖动列头边缘来调整每列的宽度，方便查看数据。

## ✅ 完成的功能

### 1. 可调整列宽
- 所有列都支持拖动调整宽度
- 拖动列头右侧边缘即可调整
- 调整后的宽度会保持在当前会话中

### 2. 固定列支持
- 全选列（左侧固定）：可调整宽度
- 操作列（右侧固定）：可调整宽度
- 中间数据列：可调整宽度

### 3. 默认列宽
每列都设置了合理的默认宽度：

| 列名 | 默认宽度 |
|------|----------|
| 单据编号 | 150px |
| 司机 | 120px |
| 车牌号 | 120px |
| 充电站 | 150px |
| 充电桩 | 120px |
| 电量(kWh) | 120px |
| 识别金额(元) | 130px |
| 计算金额(元) | 130px |
| 计算单价(元/kWh) | 150px |
| 金额差异(元) | 130px |
| 开始充电时间 | 180px |
| 结束充电时间 | 180px |
| 充电时长(分钟) | 120px |
| 充电单图片 | 100px |
| 操作 | 240px |

## 🎨 使用方法

### 调整列宽
1. 将鼠标移动到列头的右侧边缘
2. 鼠标指针会变成双向箭头 ↔️
3. 按住鼠标左键并拖动
4. 释放鼠标完成调整

### 视觉效果
```
┌─────────────────────────────────────────────────┐
│ 单据编号 ↔️ │ 司机 ↔️ │ 车牌号 ↔️ │ ... │ 操作 │
├─────────────────────────────────────────────────┤
│ 数据1      │ 数据2  │ 数据3    │ ... │ 按钮 │
└─────────────────────────────────────────────────┘
     ↑
  拖动边缘调整宽度
```

## 🔧 技术实现

### 1. 安装依赖
```bash
npm install react-resizable @types/react-resizable
```

### 2. 创建可调整大小的表头组件
**文件**：`src/components/ResizableHeaderCell.tsx`

```tsx
import React from 'react'
import { Resizable } from 'react-resizable'
import 'react-resizable/css/styles.css'

const ResizableHeaderCell: React.FC<ResizableHeaderCellProps> = (props) => {
  const { onResize, width, ...restProps } = props

  if (!width) {
    return <th {...restProps} />
  }

  return (
    <Resizable
      width={width}
      height={0}
      handle={
        <span
          className="react-resizable-handle"
          onClick={(e) => e.stopPropagation()}
        />
      }
      onResize={onResize}
      draggableOpts={{ enableUserSelectHack: false }}
    >
      <th {...restProps} />
    </Resizable>
  )
}
```

### 3. 在表格中使用

#### 状态管理
```tsx
// 列宽状态
const [columnWidths, setColumnWidths] = useState<Record<string, number>>({
  receipt_number: 150,
  driver_name: 120,
  vehicle_no: 120,
  // ... 其他列
})

// 处理列宽调整
const handleResize = (key: string) => (_e: any, { size }: any) => {
  setColumnWidths(prev => ({
    ...prev,
    [key]: size.width,
  }))
}
```

#### 列定义
```tsx
const columns = [
  {
    title: '单据编号',
    dataIndex: 'receipt_number',
    width: columnWidths.receipt_number,
    onHeaderCell: () => ({
      width: columnWidths.receipt_number,
      onResize: handleResize('receipt_number'),
    }),
  },
  // ... 其他列
]
```

#### 表格配置
```tsx
<Table
  columns={columns}
  dataSource={data}
  components={{
    header: {
      cell: ResizableHeaderCell,
    },
  }}
/>
```

## 📊 功能特性

### 1. 实时调整
- 拖动时实时显示列宽变化
- 无需刷新页面
- 流畅的用户体验

### 2. 会话保持
- 调整后的列宽在当前会话中保持
- 刷新页面后恢复默认宽度
- 未来可扩展为持久化存储

### 3. 兼容性
- 与固定列功能完全兼容
- 与排序、筛选功能兼容
- 与表头固定功能兼容

## 🎯 使用场景

### 场景1：查看长文本
某些列的内容较长（如充电站名称），可以拖宽列宽查看完整内容。

### 场景2：紧凑显示
某些列的内容较短，可以缩小列宽，在屏幕上显示更多列。

### 场景3：个性化布局
根据个人习惯调整列宽，提高工作效率。

## 📝 注意事项

### 1. 最小宽度
- 每列都有最小宽度限制（约50px）
- 防止列宽过小导致内容无法显示

### 2. 固定列
- 固定列（全选列、操作列）也可以调整宽度
- 调整固定列宽度不会影响其固定位置

### 3. 总宽度
- 调整列宽会改变表格的总宽度
- 可能需要横向滚动查看所有列

### 4. 性能
- 拖动调整列宽时性能良好
- 大数据量下也能流畅操作

## 🔄 后续优化

### 1. 持久化存储
将调整后的列宽保存到 localStorage：
```tsx
// 保存列宽
useEffect(() => {
  localStorage.setItem('charging-list-column-widths', JSON.stringify(columnWidths))
}, [columnWidths])

// 加载列宽
useEffect(() => {
  const saved = localStorage.getItem('charging-list-column-widths')
  if (saved) {
    setColumnWidths(JSON.parse(saved))
  }
}, [])
```

### 2. 重置功能
添加"重置列宽"按钮，恢复默认宽度：
```tsx
const resetColumnWidths = () => {
  setColumnWidths({
    receipt_number: 150,
    driver_name: 120,
    // ... 默认值
  })
}
```

### 3. 预设方案
提供多种列宽预设方案：
- 紧凑模式：所有列宽度较小
- 舒适模式：所有列宽度适中
- 宽松模式：所有列宽度较大

## 📦 依赖包

### react-resizable
- **版本**：^3.0.5
- **大小**：~15KB
- **功能**：提供可调整大小的组件
- **文档**：https://github.com/react-grid-layout/react-resizable

### @types/react-resizable
- **版本**：^3.0.8
- **功能**：TypeScript 类型定义

## 🚀 部署状态

```
✅ 前端构建成功
✓ 5994 modules transformed
✓ built in 6.82s
```

## 📅 更新记录

- **日期**：2026-01-26
- **版本**：v1.2
- **功能**：添加可调整列宽功能
- **状态**：✅ 已构建，待部署

---

**提示**：部署后请清除浏览器缓存以查看最新效果。拖动列头边缘即可调整列宽！



