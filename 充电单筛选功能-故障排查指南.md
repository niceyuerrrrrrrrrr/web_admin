# 充电单筛选功能 - 故障排查指南

## 🔍 问题：筛选不起作用

### 可能的原因

#### 原因1：数据还没有计算金额
**症状**：
- 筛选"异常数据"或"正常数据"时，没有数据显示
- 或者显示的数据不符合预期

**原因**：
- 充电单的 `calculated_amount`（计算金额）字段为空
- 没有计算金额就无法计算差异
- 无法判断是否异常

**解决方法**：
1. 先为充电单计算金额
2. 选择需要计算的充电单
3. 点击"批量计算金额"按钮
4. 等待计算完成
5. 再使用筛选功能

#### 原因2：浏览器缓存
**症状**：
- 筛选器显示旧的选项
- 筛选逻辑没有更新

**解决方法**：
1. 清除浏览器缓存
2. 强制刷新页面（Ctrl+F5 或 Cmd+Shift+R）
3. 重新登录系统

#### 原因3：数据字段问题
**症状**：
- 某些数据能筛选，某些不能

**可能原因**：
- `amount`（识别金额）字段为 null
- `calculated_amount`（计算金额）字段为 null
- `amount_difference`（金额差异）字段为 null

**解决方法**：
- 检查数据完整性
- 重新计算金额

## 🧪 测试筛选功能

### 测试步骤

#### 步骤1：准备测试数据
确保有以下类型的充电单：

1. **异常数据**（至少1条）
   - 识别金额：30.00
   - 计算金额：20.00
   - 差异：10.00（>5元）
   - 显示：红色加粗

2. **正常数据**（至少1条）
   - 识别金额：20.00
   - 计算金额：18.00
   - 差异：2.00（≤5元）
   - 显示：黑色正常

3. **识别金额为0**（至少1条）
   - 识别金额：0.00
   - 计算金额：15.00
   - 差异：-15.00
   - 显示：计算金额绿色

#### 步骤2：测试筛选
1. 打开充电单列表
2. 点击"金额差异(元)"列表头的筛选图标
3. 选择"异常数据(差异>5元)"
4. 检查：
   - ✅ 只显示红色加粗的数据
   - ✅ 表格底部显示正确的数量
   - ✅ 所有显示的数据差异都>5元

5. 选择"正常数据(差异≤5元)"
6. 检查：
   - ✅ 只显示黑色正常的数据
   - ✅ 所有显示的数据差异都≤5元

7. 选择"识别金额为0"
8. 检查：
   - ✅ 只显示识别金额为0的数据
   - ✅ 计算金额显示为绿色

## 🔧 调试方法

### 方法1：检查数据
打开浏览器开发者工具（F12），在Console中执行：

```javascript
// 查看表格数据
console.table(
  document.querySelectorAll('table tbody tr')
)

// 检查某条数据的字段
// 在Network标签中查看API返回的数据
```

### 方法2：检查筛选逻辑
在浏览器Console中测试筛选逻辑：

```javascript
// 模拟测试数据
const testData = [
  { amount: 30, calculated_amount: 20 }, // 差异10，应该是异常
  { amount: 20, calculated_amount: 18 }, // 差异2，应该是正常
  { amount: 0, calculated_amount: 15 },  // 识别金额为0
]

// 测试筛选逻辑
testData.forEach(record => {
  const amount = record.amount ?? 0
  const diff = amount - (record.calculated_amount ?? 0)
  const absDiff = Math.abs(diff)
  
  console.log({
    amount,
    calculated_amount: record.calculated_amount,
    diff,
    absDiff,
    isAbnormal: amount !== 0 && absDiff > 5,
    isNormal: amount !== 0 && absDiff <= 5,
    isZeroAmount: amount === 0
  })
})
```

### 方法3：查看网络请求
1. 打开开发者工具（F12）
2. 切换到 Network 标签
3. 刷新页面
4. 查找充电单数据的API请求
5. 检查返回的数据结构：
   - `amount` 字段是否存在
   - `calculated_amount` 字段是否存在
   - `amount_difference` 字段是否存在

## 📋 常见问题

### Q1: 筛选后没有数据显示
**A**: 可能原因：
1. 确实没有符合条件的数据
2. 数据还没有计算金额
3. 筛选条件太严格

**解决**：
- 先选择"全部"查看所有数据
- 检查是否有计算金额
- 如果没有，先批量计算金额

### Q2: 筛选显示的数据不对
**A**: 可能原因：
1. 浏览器缓存问题
2. 数据字段值不正确

**解决**：
- 清除缓存并刷新
- 检查数据的 amount 和 calculated_amount 字段

### Q3: 筛选器选项不显示
**A**: 可能原因：
1. 代码没有部署
2. 浏览器缓存

**解决**：
- 确认已部署最新代码
- 强制刷新页面（Ctrl+F5）

### Q4: 筛选后无法清除
**A**: 解决方法：
- 点击筛选图标，取消选中的选项
- 或选择"全部"
- 或刷新页面

## 🛠️ 修复建议

### 如果筛选完全不工作

1. **检查代码是否部署**
```bash
# 查看部署的文件时间
ls -la /var/www/html/assets/index-*.js
```

2. **清除浏览器缓存**
- Chrome: Ctrl+Shift+Delete
- 选择"缓存的图片和文件"
- 点击"清除数据"

3. **检查控制台错误**
- 打开开发者工具（F12）
- 查看Console标签
- 看是否有JavaScript错误

4. **重新部署**
```bash
cd /Users/niesiyu/Desktop/web-admin
npm run build
scp -r dist/* root@your-server:/var/www/html/
```

## 📞 联系支持

如果以上方法都无法解决问题，请提供以下信息：

1. 浏览器类型和版本
2. 是否清除了缓存
3. 开发者工具Console中的错误信息
4. 充电单数据示例（包含 amount 和 calculated_amount 字段）
5. 筛选时的具体表现（有数据但不对？还是完全没数据？）

---

**创建时间**：2026-01-26  
**版本**：v1.0.0



