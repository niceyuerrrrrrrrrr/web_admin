# 考勤异常统计功能 - 部署清单

## 修改文件清单

### 前端文件
1. **src/pages/AttendanceAnomaly.tsx**
   - 新增员工违规统计表格
   - 新增统计数据加载函数
   - 新增统计表格列定义
   - 修改查询和重置函数以同步加载统计数据

### 后端文件
1. **miniprogram_user_api/api/routers/attendance.py**
   - 新增 `/attendance/alerts/user-stats` 接口
   - 实现按用户和违规类型的分组统计
   - 支持日期范围和公司范围筛选

## 部署步骤

### 1. 前端部署

#### 步骤1：构建前端
```bash
cd /Users/niesiyu/Desktop/web-admin
npm run build
```

#### 步骤2：部署到服务器
```bash
./deploy.sh
```

或手动部署：
```bash
# 备份现有文件
ssh root@admin.hodaruner.cn "cd /www/wwwroot/admin.hodaruner.cn && cp -r dist dist.backup.$(date +%Y%m%d_%H%M%S)"

# 上传新文件
scp -r dist/* root@admin.hodaruner.cn:/www/wwwroot/admin.hodaruner.cn/dist/

# 重启Nginx（如需要）
ssh root@admin.hodaruner.cn "nginx -s reload"
```

### 2. 后端部署

#### 步骤1：上传后端文件
```bash
cd /Users/niesiyu/Desktop/pythonProject

# 备份远程文件
ssh root@your-server "cd /path/to/pythonProject && cp miniprogram_user_api/api/routers/attendance.py miniprogram_user_api/api/routers/attendance.py.backup.$(date +%Y%m%d_%H%M%S)"

# 上传修改的文件
scp miniprogram_user_api/api/routers/attendance.py root@your-server:/path/to/pythonProject/miniprogram_user_api/api/routers/
```

#### 步骤2：重启后端服务
```bash
# 方式1：使用supervisorctl
ssh root@your-server "supervisorctl restart miniprogram_user_api"

# 方式2：使用systemctl
ssh root@your-server "systemctl restart miniprogram_user_api"

# 方式3：手动重启
ssh root@your-server "pkill -f 'uvicorn.*miniprogram_user_api' && cd /path/to/pythonProject && nohup python3 -m uvicorn miniprogram_user_api.main:app --host 0.0.0.0 --port 8000 &"
```

#### 步骤3：验证服务状态
```bash
# 检查服务是否运行
ssh root@your-server "ps aux | grep uvicorn | grep miniprogram_user_api"

# 检查端口是否监听
ssh root@your-server "netstat -tlnp | grep 8000"

# 测试API接口
curl -X GET "http://your-server:8000/attendance/alerts/user-stats" -H "Authorization: Bearer YOUR_TOKEN"
```

## 测试验证

### 1. 前端测试
1. 访问管理后台：https://admin.hodaruner.cn
2. 登录管理员账号
3. 进入"考勤异常监控"页面
4. 验证页面顶部显示"员工违规统计"卡片
5. 验证统计表格正常显示数据
6. 测试日期筛选功能
7. 验证超过3次的数字是否标红

### 2. 后端测试
```bash
# 测试统计接口
curl -X GET "http://your-server:8000/attendance/alerts/user-stats" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json"

# 测试带日期范围的查询
curl -X GET "http://your-server:8000/attendance/alerts/user-stats?start_date=2026-01-01&end_date=2026-01-31" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json"

# 测试公司切换（超级管理员）
curl -X GET "http://your-server:8000/attendance/alerts/user-stats?company_id=1" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json"
```

### 3. 功能测试清单
- [ ] 统计表格正常显示
- [ ] 员工姓名和手机号正确显示
- [ ] 各类违规次数统计准确
- [ ] 总计数字正确
- [ ] 超过3次的数字标红显示
- [ ] 日期筛选功能正常
- [ ] 查询按钮功能正常
- [ ] 重置按钮功能正常
- [ ] 分页功能正常
- [ ] 权限控制正常（非管理员无法访问）
- [ ] 公司切换功能正常（超级管理员）

## 回滚方案

### 前端回滚
```bash
# 恢复备份
ssh root@admin.hodaruner.cn "cd /www/wwwroot/admin.hodaruner.cn && rm -rf dist && mv dist.backup.YYYYMMDD_HHMMSS dist"

# 重启Nginx
ssh root@admin.hodaruner.cn "nginx -s reload"
```

### 后端回滚
```bash
# 恢复备份文件
ssh root@your-server "cd /path/to/pythonProject && cp miniprogram_user_api/api/routers/attendance.py.backup.YYYYMMDD_HHMMSS miniprogram_user_api/api/routers/attendance.py"

# 重启服务
ssh root@your-server "supervisorctl restart miniprogram_user_api"
```

## 注意事项

1. **备份重要**：部署前务必备份现有文件
2. **测试环境**：建议先在测试环境验证功能
3. **数据库**：本次更新不涉及数据库变更
4. **依赖检查**：确保后端已安装所有必要的Python包
5. **权限验证**：部署后测试不同角色的权限控制
6. **性能监控**：关注统计接口的响应时间
7. **日志检查**：部署后检查后端日志是否有错误

## 监控指标

部署后需要监控以下指标：
- API响应时间（建议 < 1秒）
- 错误率（应为 0%）
- 并发请求处理能力
- 数据库查询性能

## 联系方式

如遇问题，请联系：
- 开发负责人：[姓名]
- 技术支持：[联系方式]

## 部署记录

| 日期 | 版本 | 部署人 | 状态 | 备注 |
|------|------|--------|------|------|
| 2026-01-26 | v1.0 | - | 待部署 | 初始版本 |

## 更新日期
2026-01-26



