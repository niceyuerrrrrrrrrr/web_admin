# 考勤异常统计功能 - 快速参考

## 🎯 功能简介

在考勤异常监控页面新增员工违规统计表格，按违规类型统计每位员工的考勤异常次数，超过3次自动标红。

## 📁 文件清单

### 修改的文件
```
✅ src/pages/AttendanceAnomaly.tsx                          (前端页面)
✅ miniprogram_user_api/api/routers/attendance.py          (后端接口)
```

### 新增的文档
```
📄 考勤异常统计功能说明.md          - 功能详细说明
📄 考勤异常统计-部署清单.md          - 部署步骤和测试
📄 考勤异常统计-界面说明.md          - 界面使用指南
📄 考勤异常统计-开发总结.md          - 开发总结文档
📄 deploy_attendance_stats.sh        - 自动部署脚本
📄 考勤异常统计-快速参考.md          - 本文档
```

## 🚀 快速部署

### 一键部署（推荐）
```bash
cd /Users/niesiyu/Desktop/web-admin
./deploy_attendance_stats.sh
```

### 分步部署
```bash
# 1. 构建前端
cd /Users/niesiyu/Desktop/web-admin
npm run build

# 2. 部署前端
scp -r dist/* root@admin.hodaruner.cn:/www/wwwroot/admin.hodaruner.cn/dist/

# 3. 部署后端
cd /Users/niesiyu/Desktop/pythonProject
scp miniprogram_user_api/api/routers/attendance.py root@your-server:/path/to/pythonProject/miniprogram_user_api/api/routers/

# 4. 重启后端
ssh root@your-server "supervisorctl restart miniprogram_user_api"
```

## 📊 数据结构

### API接口
```
GET /attendance/alerts/user-stats
```

### 请求参数
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| company_id | int | 否 | 公司ID（仅超级管理员） |
| start_date | string | 否 | 开始日期 YYYY-MM-DD |
| end_date | string | 否 | 结束日期 YYYY-MM-DD |

### 响应数据
```json
{
  "stats": [
    {
      "user_id": 1,
      "user_name": "张三",
      "user_phone": "13800138000",
      "late_count": 5,              // 迟到
      "early_leave_count": 2,       // 早退
      "absent_count": 1,            // 缺勤
      "location_abnormal_count": 0, // 位置异常
      "time_abnormal_count": 3,     // 时长异常
      "cross_day_count": 1,         // 跨天打卡
      "other_count": 0,             // 其他违规
      "total_count": 12             // 总计
    }
  ],
  "total_users": 10
}
```

## 🎨 界面效果

### 统计表格列
| 列名 | 说明 | 样式 |
|------|------|------|
| 员工姓名 | 固定左侧 | 普通 |
| 手机号 | - | 普通 |
| 迟到 | 迟到次数 | >3次标红 |
| 早退 | 早退次数 | >3次标红 |
| 缺勤 | 缺勤次数 | >3次标红 |
| 位置异常 | 位置异常次数 | >3次标红 |
| 时长异常 | 时长异常次数 | >3次标红 |
| 跨天打卡 | 跨天打卡次数 | >3次标红 |
| 其他违规 | 其他违规次数 | >3次标红 |
| 总计 | 固定右侧 | >3次红色粗体，≤3次蓝色粗体 |

### 视觉规则
- **标红阈值**：3次
- **标红样式**：红色粗体 `#ff4d4f`
- **总计样式**：
  - 未超标：蓝色粗体 `#1890ff`，字号16px
  - 超标：红色粗体 `#ff4d4f`，字号16px

## ✅ 测试要点

### 必测项
- [ ] 统计表格显示正常
- [ ] 数据统计准确
- [ ] 超过3次标红
- [ ] 日期筛选有效
- [ ] 权限控制正确

### 测试数据
```
员工A：迟到5次 → 迟到列标红，总计标红
员工B：早退2次 → 不标红
员工C：缺勤4次 → 缺勤列标红，总计标红
```

## 🔧 常见问题

### Q1：看不到统计表格？
**检查项**：
1. 是否有管理员权限
2. 是否选择了公司（超级管理员）
3. 后端服务是否运行
4. 浏览器控制台是否有错误

### Q2：统计数据不准确？
**解决方案**：
1. 检查日期筛选范围
2. 清空筛选条件重新查询
3. 检查后端日志

### Q3：如何修改标红阈值？
**修改位置**：
```typescript
// src/pages/AttendanceAnomaly.tsx
const isWarning = count > 3;  // 改为需要的阈值
```

### Q4：如何导出统计数据？
**当前状态**：暂不支持，可在后续版本添加

## 📞 技术支持

### 相关文档
- 详细功能说明：`考勤异常统计功能说明.md`
- 部署指南：`考勤异常统计-部署清单.md`
- 界面说明：`考勤异常统计-界面说明.md`
- 开发总结：`考勤异常统计-开发总结.md`

### 日志位置
- 前端：浏览器控制台
- 后端：`/tmp/api.log` 或 supervisorctl 日志

### 回滚命令
```bash
# 前端回滚
ssh root@admin.hodaruner.cn "cd /www/wwwroot/admin.hodaruner.cn && rm -rf dist && mv dist.backup.YYYYMMDD_HHMMSS dist"

# 后端回滚
ssh root@your-server "cd /path/to/pythonProject/miniprogram_user_api/api/routers && cp attendance.py.backup.YYYYMMDD_HHMMSS attendance.py && supervisorctl restart miniprogram_user_api"
```

## 📅 版本信息

- **版本号**：v1.0
- **开发日期**：2026-01-26
- **状态**：✅ 开发完成，待部署

---

**提示**：首次部署建议先在测试环境验证功能正常后再部署到生产环境。



