# 充电单列表数据校验功能 - 说明文档

## 📋 功能概述

为充电单列表添加智能数据校验功能，根据识别金额是否为0采用不同的校验和显示策略。

## 🎯 校验规则

### 规则1：识别金额为0的数据
- **不进行校验**
- **计算金额**：显示为绿色
- **金额差异**：显示为黑色（无特殊样式）

### 规则2：识别金额不为0的数据
- **进行校验**
- **计算金额**：显示为黑色（默认颜色）
- **金额差异**：
  - 差异 > 5元：**标红加粗** ⚠️
  - 差异 ≤ 5元：显示为黑色（正常样式）

## 📊 显示效果

### 场景1：识别金额为0
```
识别金额：￥0.00
计算金额：￥15.50 (绿色)
金额差异：-￥15.50 (黑色，正常字体)
```

### 场景2：识别金额不为0，差异≤5元
```
识别金额：￥20.00
计算金额：￥18.50 (黑色)
金额差异：+￥1.50 (黑色，正常字体) ✓
```

### 场景3：识别金额不为0，差异>5元
```
识别金额：￥30.00
计算金额：￥22.00 (黑色)
金额差异：+￥8.00 (红色，加粗) ⚠️
```

## 🔧 技术实现

### 修改的文件
- `src/pages/ChargingList.tsx`

### 修改的列

#### 1. 计算金额列
```typescript
render: (v, record: any) => {
  if (v === null || v === undefined) return '-'
  // 识别金额为0时，计算金额显示为绿色
  const amount = record.amount ?? 0
  const color = amount === 0 ? '#52c41a' : undefined
  return <span style={{ color, fontWeight: amount === 0 ? 'normal' : undefined }}>
    ￥{Number(v).toFixed(2)}
  </span>
}
```

**逻辑**：
- 检查 `record.amount`（识别金额）
- 如果为0：显示绿色
- 如果不为0：显示默认颜色（黑色）

#### 2. 金额差异列
```typescript
render: (v, record: any) => {
  // 获取识别金额
  const amount = record.amount ?? 0
  
  // 计算差异
  const diff = (record.amount_difference ?? v)
  let computed: number
  
  if (diff === null || diff === undefined) {
    if (record.amount === null || record.amount === undefined || 
        record.calculated_amount === null || record.calculated_amount === undefined) 
      return '-'
    computed = Number(record.amount) - Number(record.calculated_amount)
  } else {
    computed = Number(diff)
  }
  
  // 识别金额为0：不校验，不显示颜色
  if (amount === 0) {
    const prefix = computed > 0 ? '+' : ''
    return <span>{prefix}￥{computed.toFixed(2)}</span>
  }
  
  // 识别金额不为0：进行校验
  const absDiff = Math.abs(computed)
  let color: string | undefined
  let fontWeight: string | undefined
  
  if (absDiff > 5) {
    // 差异大于5元：标红加粗
    color = '#ff4d4f'
    fontWeight = 'bold'
  } else {
    // 差异小于等于5元：显示为黑色
    color = undefined
    fontWeight = undefined
  }
  
  const prefix = computed > 0 ? '+' : ''
  return <span style={{ color, fontWeight }}>{prefix}￥{computed.toFixed(2)}</span>
}
```

**逻辑**：
1. 获取识别金额 `amount`
2. 计算金额差异 `computed`
3. 判断识别金额是否为0：
   - 为0：不校验，显示黑色
   - 不为0：进行校验
4. 校验逻辑（识别金额不为0时）：
   - `|差异| > 5`：红色加粗
   - `|差异| ≤ 5`：黑色正常

## 📈 使用场景

### 场景1：OCR识别失败（识别金额为0）
当OCR无法识别金额时，识别金额为0。此时：
- 计算金额显示为绿色，提示用户这是计算出来的金额
- 金额差异不进行校验，因为没有参考值

### 场景2：OCR识别成功，金额基本一致
当OCR识别成功且计算准确时：
- 差异在5元以内，显示为黑色
- 表示数据正常，无需特别关注

### 场景3：OCR识别成功，金额差异较大
当OCR识别成功但计算差异较大时：
- 差异超过5元，标红加粗
- 提醒用户需要检查数据

## 🎨 颜色说明

| 颜色 | 含义 | 使用场景 |
|------|------|----------|
| 🟢 绿色 (#52c41a) | 计算值 | 识别金额为0时的计算金额 |
| ⚫ 黑色 (默认) | 正常 | 识别金额不为0且差异≤5元 |
| 🔴 红色 (#ff4d4f) | 警告 | 识别金额不为0且差异>5元 |

## 📦 构建状态

```bash
✓ 5995 modules transformed.
✓ built in 6.66s
✅ 构建成功，无错误
```

## 🚀 部署步骤

### 1. 构建
```bash
cd /Users/niesiyu/Desktop/web-admin
npm run build
```

### 2. 部署
```bash
scp -r dist/* root@your-server:/var/www/html/
```

### 3. 验证
访问充电单列表页面，检查：
- [ ] 识别金额为0的数据，计算金额显示为绿色
- [ ] 识别金额为0的数据，金额差异显示为黑色
- [ ] 识别金额不为0，差异≤5元，显示为黑色
- [ ] 识别金额不为0，差异>5元，显示为红色加粗

## 📝 测试用例

### 测试用例1：识别金额为0
| 识别金额 | 计算金额 | 金额差异 | 预期结果 |
|----------|----------|----------|----------|
| 0 | 15.50 | -15.50 | 计算金额：绿色；差异：黑色 |
| 0 | 0 | 0 | 计算金额：绿色；差异：黑色 |

### 测试用例2：识别金额不为0，差异≤5元
| 识别金额 | 计算金额 | 金额差异 | 预期结果 |
|----------|----------|----------|----------|
| 20.00 | 18.50 | +1.50 | 计算金额：黑色；差异：黑色 |
| 20.00 | 24.00 | -4.00 | 计算金额：黑色；差异：黑色 |
| 20.00 | 25.00 | -5.00 | 计算金额：黑色；差异：黑色 |

### 测试用例3：识别金额不为0，差异>5元
| 识别金额 | 计算金额 | 金额差异 | 预期结果 |
|----------|----------|----------|----------|
| 30.00 | 22.00 | +8.00 | 计算金额：黑色；差异：红色加粗 |
| 20.00 | 26.50 | -6.50 | 计算金额：黑色；差异：红色加粗 |
| 50.00 | 35.00 | +15.00 | 计算金额：黑色；差异：红色加粗 |

## ⚠️ 注意事项

1. **差异阈值**：当前设置为5元，如需调整，修改代码中的 `absDiff > 5` 判断条件

2. **识别金额判断**：使用 `record.amount ?? 0` 来判断，确保 null 和 undefined 都被视为0

3. **差异计算**：
   - 优先使用 `record.amount_difference`（如果存在）
   - 否则计算 `amount - calculated_amount`

4. **正负号显示**：
   - 正数显示 `+` 号
   - 负数自动显示 `-` 号
   - 零不显示符号

## 🔄 回滚方案

如果需要回滚到之前的版本：

```bash
# 恢复之前的显示逻辑
# 计算金额：统一显示为金色加粗
# 金额差异：正数红色，负数绿色，都加粗
```

## 📞 支持

如有问题，请联系开发团队。

---

**创建时间**：2026-01-26  
**版本**：v1.0.0  
**状态**：✅ 完成，已构建成功



