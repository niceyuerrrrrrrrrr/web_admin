# 充电单异常数据筛选功能 - 说明文档

## 🎯 功能概述

为充电单列表的"金额差异"列添加筛选器，可以快速筛选出需要关注的异常数据。

## ✨ 新增功能

### 金额差异列筛选器

在"金额差异(元)"列的表头，点击筛选图标，可以选择以下筛选条件：

#### 筛选选项

| 选项 | 说明 | 用途 |
|------|------|------|
| **全部** | 显示所有数据 | 查看全部充电单 |
| **异常数据(差异>5元)** 🔴 | 识别金额≠0 且 \|差异\| > 5元 | **重点关注**，需要人工检查修改 |
| **正常数据(差异≤5元)** ✓ | 识别金额≠0 且 \|差异\| ≤ 5元 | 数据正常，可批量处理 |
| **识别金额为0** 🟢 | 识别金额 = 0 | OCR未识别，需要确认 |

## 📊 使用场景

### 场景1：快速定位异常数据
**目标**：找出所有需要人工检查的异常充电单

**操作步骤**：
1. 点击"金额差异(元)"列表头的筛选图标
2. 选择"异常数据(差异>5元)"
3. 系统自动筛选出所有标红的异常数据
4. 逐条检查并修改

**效果**：
- 只显示差异大于5元的数据
- 这些数据在列表中显示为红色加粗
- 方便集中处理异常情况

### 场景2：批量处理正常数据
**目标**：确认正常数据，批量提交或导出

**操作步骤**：
1. 点击"金额差异(元)"列表头的筛选图标
2. 选择"正常数据(差异≤5元)"
3. 系统筛选出所有差异在可接受范围内的数据
4. 批量选择并处理

**效果**：
- 只显示差异小于等于5元的数据
- 这些数据显示为黑色正常字体
- 可以放心批量处理

### 场景3：处理OCR未识别的数据
**目标**：找出所有OCR未识别金额的充电单

**操作步骤**：
1. 点击"金额差异(元)"列表头的筛选图标
2. 选择"识别金额为0"
3. 系统筛选出所有识别金额为0的数据
4. 确认计算金额是否正确

**效果**：
- 只显示识别金额为0的数据
- 计算金额显示为绿色
- 需要人工确认计算结果

## 🎨 视觉效果

### 筛选前（全部数据）
```
序号  金额差异(元)
1     +￥1.50 (黑色)
2     +￥8.00 (红色加粗) ⚠️
3     -￥15.50 (黑色)
4     +￥3.20 (黑色)
5     +￥12.00 (红色加粗) ⚠️
```

### 筛选后（异常数据>5元）
```
序号  金额差异(元)
2     +￥8.00 (红色加粗) ⚠️
5     +￥12.00 (红色加粗) ⚠️
```

## 🔧 技术实现

### 筛选逻辑

```typescript
filters: [
  { text: '全部', value: 'all' },
  { text: '异常数据(差异>5元)', value: 'abnormal' },
  { text: '正常数据(差异≤5元)', value: 'normal' },
  { text: '识别金额为0', value: 'zero_amount' },
],
onFilter: (value, record: any) => {
  const amount = record.amount ?? 0
  const diff = record.amount_difference ?? ((record.amount ?? 0) - (record.calculated_amount ?? 0))
  const absDiff = Math.abs(diff)
  
  if (value === 'all') return true
  if (value === 'zero_amount') return amount === 0
  if (value === 'abnormal') return amount !== 0 && absDiff > 5
  if (value === 'normal') return amount !== 0 && absDiff <= 5
  return true
}
```

### 筛选条件说明

#### 1. 全部
- 不进行任何筛选
- 显示所有充电单数据

#### 2. 异常数据(差异>5元)
- 条件：`识别金额 ≠ 0` **且** `|金额差异| > 5`
- 这些数据在列表中显示为红色加粗
- 需要重点关注和人工检查

#### 3. 正常数据(差异≤5元)
- 条件：`识别金额 ≠ 0` **且** `|金额差异| ≤ 5`
- 这些数据在列表中显示为黑色正常字体
- 数据正常，可以批量处理

#### 4. 识别金额为0
- 条件：`识别金额 = 0`
- 计算金额显示为绿色
- OCR未识别到金额，需要确认计算结果

## 💡 使用技巧

### 技巧1：组合筛选
可以同时使用多个列的筛选器：
1. 先筛选"异常数据(差异>5元)"
2. 再按充电站筛选
3. 找出特定充电站的异常数据

### 技巧2：配合排序
1. 筛选"异常数据(差异>5元)"
2. 点击"金额差异(元)"列表头排序
3. 按差异大小排序，优先处理差异最大的

### 技巧3：批量导出
1. 筛选"异常数据(差异>5元)"
2. 点击"全选所有数据"
3. 点击"导出选中"
4. 导出异常数据Excel，线下处理

### 技巧4：快速统计
通过筛选器可以快速了解数据质量：
- 异常数据数量：选择"异常数据"，查看表格底部总数
- 正常数据数量：选择"正常数据"，查看表格底部总数
- OCR失败数量：选择"识别金额为0"，查看表格底部总数

## 📦 构建状态

```bash
✓ 5995 modules transformed.
✓ built in 6.81s
✅ 构建成功，无错误
```

## 🚀 部署步骤

### 1. 构建
```bash
cd /Users/niesiyu/Desktop/web-admin
npm run build
```

### 2. 部署
```bash
scp -r dist/* root@your-server:/var/www/html/
```

### 3. 验证
访问充电单列表页面，检查：
- [ ] "金额差异(元)"列表头有筛选图标
- [ ] 点击筛选图标显示4个选项
- [ ] 选择"异常数据(差异>5元)"只显示红色加粗的数据
- [ ] 选择"正常数据(差异≤5元)"只显示黑色正常的数据
- [ ] 选择"识别金额为0"只显示识别金额为0的数据
- [ ] 选择"全部"显示所有数据

## 📊 实际应用示例

### 示例1：处理异常数据工作流

```
1. 打开充电单列表
   ↓
2. 筛选"异常数据(差异>5元)"
   ↓
3. 发现10条异常数据
   ↓
4. 逐条检查：
   - 检查充电站是否正确
   - 检查充电时间是否正确
   - 检查电量是否正确
   ↓
5. 修改错误数据或重新计算
   ↓
6. 清除筛选，查看全部数据
```

### 示例2：数据质量报告

```
总充电单：1000条

筛选统计：
- 异常数据(差异>5元)：50条 (5%)  ⚠️ 需要处理
- 正常数据(差异≤5元)：900条 (90%) ✓ 数据良好
- 识别金额为0：50条 (5%)  🟢 需要确认

数据质量评分：95分（正常数据占比）
```

## ⚠️ 注意事项

1. **筛选器状态保持**
   - 筛选条件会保持，直到手动清除
   - 切换页面后筛选条件会重置

2. **与其他筛选器组合**
   - 可以同时使用多个列的筛选器
   - 筛选条件是"与"的关系（同时满足）

3. **筛选后的操作**
   - 批量选择只会选择筛选后的数据
   - 导出也只会导出筛选后的数据
   - 统计数字显示的是筛选后的总数

4. **清除筛选**
   - 点击筛选图标，取消选中的选项
   - 或点击表格右上角的"重置"按钮

## 🎯 最佳实践

### 日常数据检查流程

**每日检查**：
1. 打开充电单列表
2. 筛选"异常数据(差异>5元)"
3. 如果有数据，逐条检查并处理
4. 处理完成后，筛选"识别金额为0"
5. 确认计算金额是否合理

**批量处理**：
1. 筛选"正常数据(差异≤5元)"
2. 全选数据
3. 批量提交或导出

**数据分析**：
1. 分别筛选各个类别
2. 记录各类别数量
3. 分析数据质量趋势

## 📄 相关文档

- **充电单数据校验功能-说明文档.md** - 数据校验规则详细说明
- **充电单数据校验功能-快速参考.md** - 快速参考手册

---

**创建时间**：2026-01-26  
**版本**：v1.1.0  
**状态**：✅ 完成，已构建成功



