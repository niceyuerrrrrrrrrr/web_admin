# 考勤异常统计功能说明

## 功能概述

在考勤异常监控页面新增了**员工违规统计**功能，可以按违规类型统计每位员工的考勤异常次数，并对超过3次违规的数据进行标红提醒。

## 功能特性

### 1. 统计维度
- **员工姓名**：显示员工姓名
- **手机号**：显示员工手机号
- **违规类型分类统计**：
  - 迟到次数
  - 早退次数
  - 缺勤次数
  - 位置异常次数
  - 时长异常次数
  - 跨天打卡次数（包含可疑和严重）
  - 其他违规次数
- **总计**：所有违规类型的总次数

### 2. 视觉提醒
- **超过3次标红**：任何违规类型超过3次，该数字会以红色粗体显示
- **总计突出显示**：总计列使用蓝色粗体（未超标）或红色粗体（超标）显示，字号更大

### 3. 筛选功能
- 支持按日期范围筛选统计数据
- 支持公司切换（超级管理员）
- 与下方的违规明细列表使用相同的筛选条件

### 4. 排序规则
- 默认按总违规次数从高到低排序
- 方便快速识别违规次数最多的员工

## 技术实现

### 前端实现
**文件**：`src/pages/AttendanceAnomaly.tsx`

#### 新增状态
```typescript
const [statsLoading, setStatsLoading] = useState(false);
const [userStats, setUserStats] = useState<any[]>([]);
```

#### 新增API调用
```typescript
const loadUserStats = async () => {
  // 调用 /attendance/alerts/user-stats 接口
  // 支持 company_id, start_date, end_date 参数
}
```

#### 统计表格列定义
- 固定左侧列：员工姓名
- 固定右侧列：总计
- 中间列：各类违规次数
- 所有数字列居中对齐
- 超过3次的数字标红加粗

### 后端实现
**文件**：`miniprogram_user_api/api/routers/attendance.py`

#### 新增接口
```
GET /attendance/alerts/user-stats
```

#### 请求参数
- `company_id` (可选)：公司ID，仅超级管理员可用
- `start_date` (可选)：开始日期，格式 YYYY-MM-DD
- `end_date` (可选)：结束日期，格式 YYYY-MM-DD

#### 响应格式
```json
{
  "code": 200,
  "success": true,
  "message": "获取员工违规统计成功",
  "data": {
    "stats": [
      {
        "user_id": 1,
        "user_name": "张三",
        "user_phone": "13800138000",
        "late_count": 5,
        "early_leave_count": 2,
        "absent_count": 1,
        "location_abnormal_count": 0,
        "time_abnormal_count": 3,
        "cross_day_count": 1,
        "other_count": 0,
        "total_count": 12
      }
    ],
    "total_users": 10
  }
}
```

#### 实现逻辑
1. 权限检查：仅管理员可访问
2. 公司范围过滤：根据用户权限和参数确定查询范围
3. 日期范围过滤：支持按时间段统计
4. SQL分组查询：按 user_id 和 alert_type 分组统计
5. 数据聚合：将同一用户的不同违规类型汇总
6. 排序：按总违规次数降序排列

## 使用说明

### 1. 查看统计
1. 进入"考勤异常监控"页面
2. 页面顶部显示"员工违规统计"卡片
3. 表格展示所有员工的违规统计数据

### 2. 筛选数据
1. 使用日期范围选择器选择统计时间段
2. 点击"查询"按钮刷新统计数据
3. 统计表格和下方明细列表会同步更新

### 3. 识别问题员工
1. 查看总计列，快速识别违规次数最多的员工
2. 红色标注的数字表示该类型违规超过3次
3. 可以结合下方明细列表查看具体违规记录

## 部署说明

### 前端部署
```bash
cd /Users/niesiyu/Desktop/web-admin
npm run build
./deploy.sh
```

### 后端部署
后端代码已更新，需要重启服务：
```bash
# 在服务器上执行
cd /path/to/pythonProject
supervisorctl restart miniprogram_user_api
# 或
systemctl restart miniprogram_user_api
```

## 注意事项

1. **权限要求**：只有管理员角色（车队长、统计、财务、总经理、管理员、超级管理员）可以查看统计数据
2. **数据范围**：普通管理员只能查看本公司数据，超级管理员可以切换公司查看
3. **性能考虑**：统计数据使用SQL分组查询，性能较好，但数据量大时建议使用日期范围筛选
4. **标红阈值**：当前阈值为3次，如需调整可修改前端代码中的判断条件

## 后续优化建议

1. **可配置阈值**：将标红阈值改为可配置项
2. **导出功能**：支持导出统计数据为Excel
3. **趋势分析**：增加时间维度的趋势图表
4. **预警通知**：对超标员工自动发送提醒通知
5. **钻取功能**：点击统计数字可直接查看该员工的违规明细

## 更新日期
2026-01-26



