# 充电单数据校验功能 - 快速参考

## ✅ 完成状态

已成功为充电单列表添加智能数据校验功能！

## 🎯 校验规则（一图看懂）

```
识别金额 = 0？
    ├─ 是 → 不校验
    │       ├─ 计算金额：🟢 绿色
    │       └─ 金额差异：⚫ 黑色
    │
    └─ 否 → 进行校验
            ├─ 计算金额：⚫ 黑色
            └─ 金额差异：
                ├─ |差异| > 5元 → 🔴 红色加粗 ⚠️
                └─ |差异| ≤ 5元 → ⚫ 黑色
```

## 📊 显示效果示例

### 场景1：识别金额为0（OCR失败）
```
识别金额：￥0.00
计算金额：￥15.50 🟢 (绿色)
金额差异：-￥15.50 ⚫ (黑色)
```
**含义**：OCR未识别到金额，显示计算值供参考

### 场景2：差异小（正常）
```
识别金额：￥20.00
计算金额：￥18.50 ⚫ (黑色)
金额差异：+￥1.50 ⚫ (黑色) ✓
```
**含义**：数据正常，差异在可接受范围内

### 场景3：差异大（需检查）
```
识别金额：￥30.00
计算金额：￥22.00 ⚫ (黑色)
金额差异：+￥8.00 🔴 (红色加粗) ⚠️
```
**含义**：差异较大，需要人工检查

## 🔧 技术要点

### 修改的文件
- `src/pages/ChargingList.tsx`

### 核心逻辑

#### 计算金额列
```typescript
const amount = record.amount ?? 0
const color = amount === 0 ? '#52c41a' : undefined
```

#### 金额差异列
```typescript
if (amount === 0) {
  // 不校验
  return <span>{prefix}￥{computed.toFixed(2)}</span>
}

// 校验
const absDiff = Math.abs(computed)
if (absDiff > 5) {
  color = '#ff4d4f'  // 红色
  fontWeight = 'bold'
} else {
  color = undefined  // 黑色
}
```

## 📦 构建状态

```bash
✓ built in 6.66s
✅ 构建成功
```

## 🚀 快速部署

```bash
cd /Users/niesiyu/Desktop/web-admin
npm run build
scp -r dist/* root@your-server:/var/www/html/
```

## ✅ 验证清单

- [ ] 识别金额=0，计算金额显示绿色
- [ ] 识别金额=0，金额差异显示黑色
- [ ] 识别金额≠0，差异≤5元，显示黑色
- [ ] 识别金额≠0，差异>5元，显示红色加粗

## 🎨 颜色含义

| 颜色 | 含义 | 场景 |
|------|------|------|
| 🟢 绿色 | 计算值 | 识别金额为0 |
| ⚫ 黑色 | 正常 | 差异≤5元 |
| 🔴 红色 | 警告 | 差异>5元 |

## 💡 使用建议

1. **绿色计算金额**：表示OCR未识别，需要人工确认
2. **黑色差异**：数据正常，可以批量处理
3. **红色差异**：重点关注，逐条检查

## 📝 快速测试

访问充电单列表，查找：
1. 识别金额为0的记录 → 计算金额应为绿色
2. 差异大于5元的记录 → 金额差异应为红色加粗
3. 差异小于5元的记录 → 金额差异应为黑色

---

**完成时间**：2026-01-26  
**状态**：✅ 完成



