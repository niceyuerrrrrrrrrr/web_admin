# 考勤异常统计功能 - 界面说明

## 页面布局

考勤异常监控页面现在分为两个主要部分：

### 1. 员工违规统计（新增）
位于页面顶部，展示所有员工的违规统计汇总数据。

#### 界面元素
```
┌─────────────────────────────────────────────────────────────────┐
│ 👤 员工违规统计  [超过3次标红]                                    │
├─────────────────────────────────────────────────────────────────┤
│ 员工姓名 │ 手机号      │ 迟到 │ 早退 │ 缺勤 │ 位置异常 │ ... │ 总计 │
├─────────────────────────────────────────────────────────────────┤
│ 张三     │ 13800138000 │  5   │  2   │  1   │    0     │ ... │  12  │
│ 李四     │ 13800138001 │  2   │  1   │  0   │    1     │ ... │   4  │
│ 王五     │ 13800138002 │  1   │  0   │  0   │    0     │ ... │   1  │
└─────────────────────────────────────────────────────────────────┘
```

#### 列说明
- **员工姓名**：固定在左侧，方便查看
- **手机号**：员工联系方式
- **迟到**：迟到次数统计
- **早退**：早退次数统计
- **缺勤**：缺勤次数统计
- **位置异常**：打卡位置不在规定范围内的次数
- **时长异常**：工作时长异常的次数
- **跨天打卡**：跨天打卡次数（包含可疑和严重）
- **其他违规**：其他类型违规次数
- **总计**：固定在右侧，所有违规类型的总和

#### 视觉特性
1. **标红规则**：任何违规类型超过3次，该数字显示为红色粗体
2. **总计突出**：
   - 未超标（≤3次）：蓝色粗体，字号16px
   - 超标（>3次）：红色粗体，字号16px
3. **排序**：默认按总计从高到低排序
4. **分页**：每页显示10条，支持切换每页数量

### 2. 违规事件明细（原有）
位于统计表格下方，展示每一条违规事件的详细信息。

#### 界面元素
```
┌─────────────────────────────────────────────────────────────────┐
│ 考勤异常监控  [展示每一条违规事件明细]                            │
├─────────────────────────────────────────────────────────────────┤
│ [违规类型▼] [处理状态▼] [开始日期 → 结束日期] [查询] [重置]      │
├─────────────────────────────────────────────────────────────────┤
│ ID │ 用户信息 │ 违规类型 │ 严重程度 │ 违规描述 │ 发生时间 │ 操作 │
├─────────────────────────────────────────────────────────────────┤
│ 47 │ 张三     │ 迟到     │ 中       │ 上班...  │ 2026-... │ 详情 │
│ 46 │ 李四     │ 早退     │ 高       │ 下班...  │ 2026-... │ 详情 │
└─────────────────────────────────────────────────────────────────┘
```

## 筛选功能

### 筛选条件
1. **违规类型**：下拉选择
   - 迟到
   - 早退
   - 缺勤
   - 位置异常
   - 时长异常
   - 跨天打卡(可疑)
   - 跨天补打卡(严重)
   - 长时间未打下班卡
   - 连续快速打卡

2. **处理状态**：下拉选择
   - 未处理
   - 已处理

3. **日期范围**：日期选择器
   - 开始日期
   - 结束日期

### 筛选行为
- 点击"查询"按钮：同时刷新统计表格和明细列表
- 点击"重置"按钮：清空所有筛选条件，重新加载数据
- 日期筛选会同时应用到统计和明细

## 使用场景

### 场景1：快速识别问题员工
1. 打开考勤异常监控页面
2. 查看统计表格顶部的员工
3. 红色数字表示该类型违规超过3次
4. 总计列红色表示总违规次数超过3次

### 场景2：查看特定时间段的违规情况
1. 选择日期范围（如：本月）
2. 点击"查询"按钮
3. 统计表格显示该时间段内的违规统计
4. 明细列表显示该时间段内的违规记录

### 场景3：分析某个员工的违规详情
1. 在统计表格中找到该员工
2. 查看各类违规次数分布
3. 在明细列表中筛选该员工的记录
4. 点击"查看详情"了解具体违规情况

### 场景4：关注特定类型的违规
1. 在违规类型下拉框选择类型（如：迟到）
2. 点击"查询"按钮
3. 统计表格显示所有员工的迟到次数
4. 明细列表只显示迟到记录

## 数据说明

### 统计规则
- **统计范围**：根据用户权限和公司范围确定
  - 普通管理员：只能查看本公司数据
  - 超级管理员：可以切换公司查看
- **统计周期**：默认显示所有历史数据，可通过日期筛选限定范围
- **实时性**：每次查询都从数据库实时统计，确保数据准确

### 违规类型映射
后端数据库中的违规类型与统计表格列的对应关系：
- `late` → 迟到
- `early_leave` → 早退
- `absent` → 缺勤
- `location_abnormal` → 位置异常
- `time_abnormal` → 时长异常
- `cross_day_checkout` + `cross_day_checkout_suspicious` → 跨天打卡
- 其他类型 → 其他违规

## 权限说明

### 访问权限
只有以下角色可以查看统计数据：
- 车队长
- 统计
- 财务
- 总经理
- 管理员
- 超级管理员

### 数据范围权限
- **普通管理员**：只能查看本公司员工的数据
- **超级管理员**：可以通过公司选择器切换查看不同公司的数据

## 性能优化

### 前端优化
- 统计表格和明细列表独立加载，互不影响
- 使用分页减少单次渲染数据量
- 表格列固定，提升大数据量下的滚动体验

### 后端优化
- 使用SQL分组查询，一次查询获取所有统计数据
- 在应用层聚合数据，减少数据库压力
- 支持日期范围筛选，避免全表扫描

## 常见问题

### Q1：为什么统计数据和明细数量不一致？
A：统计数据是按用户聚合的，一个用户可能有多条违规记录。明细列表显示的是每一条违规记录。

### Q2：如何修改标红阈值？
A：修改前端代码 `AttendanceAnomaly.tsx` 中的判断条件 `count > 3`，改为需要的阈值。

### Q3：统计数据多久更新一次？
A：每次点击"查询"按钮或页面加载时都会实时从数据库查询最新数据。

### Q4：可以导出统计数据吗？
A：当前版本暂不支持导出，可在后续版本中添加此功能。

### Q5：为什么看不到统计表格？
A：请检查：
1. 是否有管理员权限
2. 是否选择了公司（超级管理员）
3. 后端服务是否正常运行
4. 浏览器控制台是否有错误信息

## 更新日期
2026-01-26



